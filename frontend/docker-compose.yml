version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - backend

  backend:
    build: ../egocentric_construction_partner-main
    ports:
      - "8000:8000"
